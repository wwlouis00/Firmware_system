#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "math_utils.h"

int main(void) {

    int a = 5, b = 3;
    printf("a + b = %d\n", add(a, b));
    printf("a * b = %d\n", multiply(a, b));
    printf( "\n ========================================\n" );
    printf( " LOG UART PORT " );
    printf( "\n ========================================\n" );
    return 0;
}
